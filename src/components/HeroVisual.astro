<div class="hero-visual" aria-hidden="true">
  <div class="hero-visual__glow"></div>
  <div class="hero-visual__window">
    <div class="hero-visual__toolbar">
      <span class="hero-visual__dot"></span>
      <span class="hero-visual__dot"></span>
      <span class="hero-visual__dot"></span>
      <span class="hero-visual__toolbar-title">ViewDeck — Pendulum lesson</span>
    </div>
    <div class="hero-visual__body">
      <div class="hero-visual__sidebar">
        <div class="hero-visual__sidebar-block"></div>
        <div class="hero-visual__sidebar-block hero-visual__sidebar-block--short"></div>
        <div class="hero-visual__sidebar-block hero-visual__sidebar-block--short"></div>
      </div>
      <div class="hero-visual__canvas">
        <svg viewBox="0 0 320 200" class="hero-visual__svg">
          <defs>
            <linearGradient id="arcGrad" x1="0%" y1="0%" x2="100%" y2="100%">
              <stop offset="0%" stop-color="#2d70b3" stop-opacity="0.15" />
              <stop offset="100%" stop-color="#6366f1" stop-opacity="0.05" />
            </linearGradient>
          </defs>
          <rect width="320" height="200" fill="url(#arcGrad)" rx="8" />
          <line x1="160" y1="40" x2="120" y2="130" stroke="#2d70b3" stroke-width="2.5" stroke-linecap="round" />
          <circle cx="120" cy="130" r="14" fill="#2d70b3" opacity="0.9" />
          <path d="M 120 130 Q 200 90 240 130" stroke="#6366f1" stroke-width="1.5" fill="none" stroke-dasharray="4 4" opacity="0.5" />
          <circle cx="240" cy="130" r="8" fill="#6366f1" opacity="0.4" />
        </svg>
      </div>
      <div class="hero-visual__props">
        <div class="hero-visual__prop-row"><span>Length</span><span class="hero-visual__pill">1.2 m</span></div>
        <div class="hero-visual__prop-row"><span>Angle</span><span class="hero-visual__pill">30°</span></div>
      </div>
    </div>
    <div class="hero-visual__timeline">
      <div class="hero-visual__play"></div>
      <div class="hero-visual__track"><div class="hero-visual__progress"></div></div>
    </div>
  </div>
</div>

<style>
  .hero-visual {
    position: relative;
    width: 100%;
    max-width: 520px;
    margin-inline: auto;
  }

  .hero-visual__glow {
    position: absolute;
    inset: -20% -10%;
    background: radial-gradient(ellipse at 50% 50%, rgba(45, 112, 179, 0.25), transparent 70%);
    pointer-events: none;
  }

  .hero-visual__window {
    position: relative;
    background: var(--ss-bg-panel);
    border: 1px solid rgba(255, 255, 255, 0.8);
    border-radius: 16px;
    box-shadow:
      0 4px 6px rgba(15, 23, 42, 0.04),
      0 24px 48px rgba(15, 23, 42, 0.12),
      0 0 0 1px rgba(45, 112, 179, 0.08);
    overflow: hidden;
  }

  .hero-visual__toolbar {
    display: flex;
    align-items: center;
    gap: 6px;
    padding: 10px 14px;
    background: linear-gradient(180deg, #f8fafc, #f1f5f9);
    border-bottom: 1px solid var(--ss-border);
  }

  .hero-visual__dot {
    width: 8px;
    height: 8px;
    border-radius: 50%;
    background: #cbd5e1;
  }

  .hero-visual__dot:first-child { background: #f87171; }
  .hero-visual__dot:nth-child(2) { background: #fbbf24; }
  .hero-visual__dot:nth-child(3) { background: #34d399; }

  .hero-visual__toolbar-title {
    margin-left: 8px;
    font-size: 0.7rem;
    font-weight: 600;
    color: var(--ss-text-muted);
  }

  .hero-visual__body {
    display: grid;
    grid-template-columns: 72px 1fr 88px;
    gap: 0;
    min-height: 200px;
  }

  .hero-visual__sidebar {
    padding: 10px 8px;
    background: #1e293b;
    display: flex;
    flex-direction: column;
    gap: 6px;
  }

  .hero-visual__sidebar-block {
    height: 28px;
    background: #334155;
    border-radius: 6px;
  }

  .hero-visual__sidebar-block--short {
    height: 18px;
    opacity: 0.7;
  }

  .hero-visual__canvas {
    padding: 12px;
    background: #94a3b8;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .hero-visual__svg {
    width: 100%;
    height: auto;
    border-radius: 8px;
    background: #fff;
  }

  .hero-visual__props {
    padding: 10px 8px;
    background: #f8fafc;
    border-left: 1px solid var(--ss-border);
    font-size: 0.65rem;
  }

  .hero-visual__prop-row {
    display: flex;
    flex-direction: column;
    gap: 4px;
    margin-bottom: 10px;
    color: var(--ss-text-secondary);
    font-weight: 500;
  }

  .hero-visual__pill {
    display: inline-block;
    padding: 3px 8px;
    background: var(--ss-accent-muted);
    color: var(--ss-accent);
    border-radius: 999px;
    font-weight: 700;
    width: fit-content;
  }

  .hero-visual__timeline {
    display: flex;
    align-items: center;
    gap: 10px;
    padding: 10px 14px;
    border-top: 1px solid var(--ss-border);
    background: #fff;
  }

  .hero-visual__play {
    width: 24px;
    height: 24px;
    border-radius: 50%;
    background: var(--ss-accent);
    flex-shrink: 0;
  }

  .hero-visual__track {
    flex: 1;
    height: 4px;
    background: #e2e8f0;
    border-radius: 999px;
    overflow: hidden;
  }

  .hero-visual__progress {
    width: 45%;
    height: 100%;
    background: linear-gradient(90deg, var(--ss-accent), #6366f1);
    border-radius: 999px;
  }
</style>
